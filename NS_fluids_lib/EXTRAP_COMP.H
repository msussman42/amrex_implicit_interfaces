#ifndef EXTRAP_COMP_H_
#define EXTRAP_COMP_H_

#include <DRAG_COMP.H>

#define ENUM_T11 (0)
#define ENUM_T12 (1)
#define ENUM_T22 (2)
#define ENUM_T33 (3)
#define ENUM_T13 (4)
#define ENUM_T23 (5)
#define ENUM_NUM_TENSOR_TYPE (2*AMREX_SPACEDIM)

#define ENUM_XVEL (0)
#define ENUM_YVEL (1)
#define ENUM_ZVEL (2)
#define ENUM_PRESVAR (AMREX_SPACEDIM)

#define ENUM_VOFVAR (0)
#define ENUM_CENXVAR (1)
#define ENUM_CENYVAR (2)
#define ENUM_CENZVAR (3)
#define ENUM_ORDERVAR (AMREX_SPACEDIM+1)
#define ENUM_NXVAR (ENUM_ORDERVAR+1)
#define ENUM_NYVAR (ENUM_NXVAR+1)
#define ENUM_NZVAR (ENUM_NYVAR+1)
#define ENUM_LSVAR (2*AMREX_SPACEDIM+2)
#define ENUM_NUM_MOF_VAR (ENUM_LSVAR+1)

#define ENUM_DENVAR (0)
#define ENUM_TEMPERATUREVAR (1)
#define ENUM_SPECIESVAR (2)

#define EXTRAP_PER_BURNING (AMREX_SPACEDIM)
#if (0)
interface temperature and interface mass fraction.
#endif
#define EXTRAP_PER_TSAT (2)

#define EXTRAP_NCOMP_BURNING (num_interfaces+num_interfaces*EXTRAP_PER_BURNING)
#define EXTRAP_NCOMP_TSAT (num_interfaces+num_interfaces*EXTRAP_PER_TSAT)
#if (0)
11,12,22,33,13,23,XD,YD,ZD
#endif
#define EXTRAP_NCOMP_ELASTIC (ENUM_NUM_TENSOR_TYPE+AMREX_SPACEDIM)
#define EXTRAP_NCOMP_SOLID (1+AMREX_SPACEDIM)

#define EXTRAPCOMP_SCALAR (0)
#define EXTRAPCOMP_VELOCITY (EXTRAPCOMP_SCALAR+1)
#define EXTRAPCOMP_MOF (EXTRAPCOMP_VELOCITY+AMREX_SPACEDIM)
#define EXTRAPCOMP_MASK (EXTRAPCOMP_MOF+num_materials*ngeom_recon)
#define EXTRAPCOMP_BURNVEL (EXTRAPCOMP_MASK+1)
#define EXTRAPCOMP_TSAT (EXTRAPCOMP_BURNVEL+EXTRAP_NCOMP_BURNING)
#define EXTRAPCOMP_ELASTIC (EXTRAPCOMP_TSAT+EXTRAP_NCOMP_TSAT)
#define EXTRAPCOMP_DRAG (EXTRAPCOMP_ELASTIC+EXTRAP_NCOMP_ELASTIC)
#define EXTRAP_NCOMP (EXTRAPCOMP_DRAG+N_DRAG)

#define STATECOMP_VEL (0)
#define STATECOMP_PRES (STATECOMP_VEL+AMREX_SPACEDIM)
#define STATECOMP_STATES (STATECOMP_PRES+1)
#define STATECOMP_MOF (STATECOMP_STATES+num_materials*num_state_material)
#define STATECOMP_ERR (STATECOMP_MOF+num_materials*ngeom_raw)
#define STATE_NCOMP (STATECOMP_ERR+1)

#define FACECOMP_CURV (0)
#define FACECOMP_PFORCE (FACECOMP_CURV+1)
#define FACECOMP_FACEDEN (FACECOMP_PFORCE+1)
#define FACECOMP_FACECUT (FACECOMP_FACEDEN+1)
#define FACECOMP_ICEFACECUT (FACECOMP_FACECUT+1)
#define FACECOMP_ICEMASK (FACECOMP_ICEFACECUT+1)
#define FACECOMP_FACEVISC (FACECOMP_ICEMASK+1)
#define FACECOMP_FACEHEAT (FACECOMP_FACEVISC+1)
#define FACECOMP_FACEVEL (FACECOMP_FACEHEAT+1)
#define FACECOMP_FACESPEC (FACECOMP_FACEVEL+1)
#define FACECOMP_FACESMOOTH (FACECOMP_FACESPEC+num_species_var)
#define FACECOMP_MASSFACE (FACECOMP_FACESMOOTH+1)
#define FACECOMP_VOFFACE (FACECOMP_MASSFACE+2*num_materials)
#define FACECOMP_NEXT (FACECOMP_VOFFACE+2*num_materials)
#define FACECOMP_NCOMP (FACECOMP_NEXT)

#define SOLVETYPE_PRES 0
#define SOLVETYPE_INITPROJ 1
#define SOLVETYPE_HEAT 2
#define SOLVETYPE_VISC 3
#define SOLVETYPE_PRESCOR 11
#define SOLVETYPE_PRESEXTRAP 12
#define SOLVETYPE_SPEC 100
#define SOLVETYPE_SMOOTH 200

#define SEM_U 0
#define SEM_V 1
#define SEM_W (AMREX_SPACEDIM-1)
#define SEM_T (SEM_W+1)
#define SEM_NEXT (SEM_T+1)
#define NFLUXSEM (SEM_NEXT)

#define SEMADVECT 0
#define SEMDIFFUSE_T (SEMADVECT+NFLUXSEM)
#define SEMDIFFUSE_U (SEMDIFFUSE_T+1)
#define SEMPROCESS_NEXT (SEMDIFFUSE_U+AMREX_SPACEDIM)
#define NSTATE_SDC (SEMPROCESS_NEXT)

#define FSI_VELOCITY 0
#define FSI_LEVELSET (FSI_VELOCITY+3)
#define FSI_TEMPERATURE (FSI_LEVELSET+1)
#define FSI_EXTRAP_FLAG (FSI_TEMPERATURE+1)
#define FSI_FORCE (FSI_EXTRAP_FLAG+1)
#define FSI_NEXT (FSI_FORCE+3)
#define NCOMP_FSI (FSI_NEXT)

#endif
