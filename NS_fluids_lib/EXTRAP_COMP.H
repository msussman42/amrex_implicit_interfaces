#ifndef EXTRAP_COMP_H_
#define EXTRAP_COMP_H_

#include <DRAG_COMP.H>

#define ENUM_T11 (0)
#define ENUM_T12 (1)
#define ENUM_T22 (2)
#define ENUM_T33 (3)
#define ENUM_T13 (4)
#define ENUM_T23 (5)
#define ENUM_NUM_TENSOR_TYPE (2*AMREX_SPACEDIM)

#define ENUM_XVEL (0)
#define ENUM_YVEL (1)
#define ENUM_ZVEL (2)
#define ENUM_PRESVAR (AMREX_SPACEDIM)

#define ENUM_VOFVAR (0)
#define ENUM_CENXVAR (1)
#define ENUM_CENXVAR (2)
#define ENUM_CENZVAR (3)
#define ENUM_ORDERVAR (AMREX_SPACEDIM+1)
#define ENUM_NXVAR (ENUM_ORDERVAR+1)
#define ENUM_NYVAR (ENUM_NXVAR+1)
#define ENUM_NZVAR (ENUM_NYVAR+1)
#define ENUM_LSVAR (2*AMREX_SPACEDIM+2)
#define ENUM_NUM_MOF_VAR (ENUM_LS_VAR+1)

#define ENUM_DENVAR (0)
#define ENUM_TEMPERATUREVAR (1)
#define ENUM_SPECIESVAR (2)

#define EXTRAP_PER_BURNING (AMREX_SPACEDIM)
#if (0)
interface temperature and interface mass fraction.
#endif
#define EXTRAP_PER_TSAT (2)

#define EXTRAP_NCOMP_BURNING (num_interfaces+num_interfaces*EXTRAP_PER_BURNING)
#define EXTRAP_NCOMP_TSAT (num_interfaces+num_interfaces*EXTRAP_PER_TSAT)
#if (0)
11,12,22,33,13,23,XD,YD,ZD
#endif
#define EXTRAP_NCOMP_ELASTIC (ENUM_NUM_TENSOR_TYPE+AMREX_SPACEDIM)
#define EXTRAP_NCOMP_SOLID (1+AMREX_SPACEDIM)

#define EXTRAP_SCALAR (0)
#define EXTRAP_VEL (EXTRAP_SCALAR+1)
#define EXTRAP_RECON (EXTRAP_VEL+AMREX_SPACEDIM)
#define EXTRAP_MASK (EXTRAP_RECON+num_materials*ngeom_recon);
#define EXTRAP_BURNING (EXTRAP_MASK+1)
#define EXTRAP_TSAT (EXTRAP_BURNING+EXTRAP_NCOMP_BURNING)
#define EXTRAP_ELASTIC (EXTRAP_TSAT+EXTRAP_NCOMP_TSAT)

#define EXTRAPCOMP_SCALAR (0)
#define EXTRAPCOMP_VELOCITY (EXTRAPCOMP_SCALAR+1)
#define EXTRAPCOMP_MOF (EXTRAPCOMP_VELOCITY+AMREX_SPACEDIM)
#define EXTRAPCOMP_MASK (EXTRAPCOMP_MOF+num_materials*ngeom_recon)
#define EXTRAPCOMP_BURNVEL (EXTRAPCOMP_MASK+1)
#define EXTRAPCOMP_TSAT (EXTRAPCOMP_BURNVEL+EXTRAP_NCOMP_BURNING)
#define EXTRAPCOMP_ELASTIC (EXTRAPCOMP_TSAT+EXTRAP_NCOMP_TSAT)
#define EXTRAPCOMP_DRAG (EXTRAPCOMP_ELASTIC+EXTRAP_NCOMP_ELASTIC)
#define EXTRAP_NCOMP (EXTRAPCOMP_DRAG+N_DRAG)

#define STATECOMP_VEL (0)
#define STATECOMP_PRES (STATECOMP_VEL+AMREX_SPACEDIM)
#define STATECOMP_STATES (STATECOMP_PRES+1)
#define STATECOMP_MOF (STATECOMP_STATES+num_materials*num_state_material)
#define STATECOMP_ERR (STATECOMP_MOF+num_materials*ngeom_raw)
#define STATE_NCOMP (STATECOMP_ERR+1)

#endif
